[project]
name = "email-cleaning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ruff>=0.11.7",
    "tqdm>=4.67.1",
]


[tool.ruff.lint]
preview = true
extend-select = [
    "E",
    "F",
    "I",
]
